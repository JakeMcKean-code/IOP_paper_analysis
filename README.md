# IOP_paper_analysis
Analysis of NUISANCE flat trees to investigate effect of FSI on HK and DUNE. Scripts use python uproot to open the flat trees and matplotlib to plot the results. 
The plotting style is chosen as `nature` and requres `LaTeX` installed so it can render LaTeX strings on the plots. 


